# 📝 Git Commands - Quick Reference

## Initial Setup (First Time)

```bash
# Navigate to your project root
cd D:\DatingApp\OneTimeChat

# Initialize git (if not already done)
git init

# Add all files
git add .

# Commit
git commit -m "Initial commit - Frontend and Backend setup"

# Create repository on GitHub first, then:
git remote add origin https://github.com/YOUR_USERNAME/onetime-chat.git

# Push to GitHub
git branch -M main
git push -u origin main
```

---

## Daily Commits (After Changes)

```bash
# Check what changed
git status

# Add all changes
git add .

# Commit with message
git commit -m "Your commit message here"

# Push to GitHub
git push
```

---

## Common Commits

```bash
# After adding new features
git add .
git commit -m "Add new feature"
git push

# After fixing bugs
git add .
git commit -m "Fix bug"
git push

# After updating dependencies
git add .
git commit -m "Update dependencies"
git push
```

---

## Important Notes

### ✅ Files That Are Committed:
- Frontend code (`app/`, `components/`, etc.)
- Backend code (`backend/` folder)
- Configuration files (`package.json`, `next.config.js`, etc.)
- Documentation files

### ❌ Files That Are NOT Committed (in .gitignore):
- `.env` files (backend secrets)
- `.env.local` files (frontend secrets)
- `node_modules/` folders
- `.next/` build folder
- Log files

---

## Repository Structure for Git

```
OneTimeChat/
├── app/              ✅ Committed
├── components/       ✅ Committed
├── backend/          ✅ Committed
├── package.json      ✅ Committed
├── .env              ❌ NOT committed (secrets)
├── .env.local        ❌ NOT committed (secrets)
└── node_modules/     ❌ NOT committed
```

---

## Quick Start

**Just run these commands in your project root:**

```bash
git add .
git commit -m "Setup complete - Frontend and Backend"
git push
```

**That's it!** Both frontend and backend are committed together.

